//
// Created by Lee ChenZe on 5/31/25.
//
#include <iostream>;
#include <fstream>;
#include <string>;

using namespace std;

void write_binary_file() {
    cout << endl << "========================write_binary_file========================" << endl;

    // string filename = "/Users/lee/MySkills/The-self-cultivation-of-a-programmer-CProgram/Cpp/021CppFileOperate/fileOutDir/binary.txt";
    // ofstream fout;
    //
    // // 意思是以二进制的方式写入文件，并且写入的数据是以追加到末尾的形式（ios::app)
    // fout.open(filename, ios::app | ios::binary);
    //
    // if(fout.is_open() == false) {
    //     cout << "打开文件" << filename << "失败" << endl;
    // }
    //
    // // 定义一个结构体，用于存放超女数据
    // struct st_boy {
    //     char name[31];
    //     int no;
    //     char memo[301];
    //     double weight;
    // } boy;
    //
    // // 向文件写入二进制数据
    // boy = {"douglas", 1, "一条无用的备注", 45.5};
    // fout.write((const char *) &boy, sizeof(st_boy));
    // boy = {"hamilton", 2, "备注：汉密尔顿", 65.5};
    // fout.write((const char *) &boy, sizeof(st_boy));
    //
    // // 关闭文件
    // fout.close();
    //
    // cout << "操作文件完成" << endl;

    cout << endl << "========================write_binary_file========================" << endl;
}
